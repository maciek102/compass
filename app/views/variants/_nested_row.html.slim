/ Tabela wariantów produktu renderowana nested pod wierszem produktu
/ locals: variants - kolekcja wariantów do wyświetlenia

table.index-table
  tbody
    - variants.each do |el|
      tr.table-row.nested-row data-url=variant_path(el)
        td colspan=2
        td = el.sku
        td
          = fast_link(product_path(el.product), text: el.product.name)
          = inline_slash_separator
          = fast_link(variant_path(el)) do 
            strong = el.name
        td = "Stan: #{el.stock}"
        td = "Rezerwacje: #{el.reserved_count}"
        td = "Cena: #{show_price(el.price)}"