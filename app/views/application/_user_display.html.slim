.user-display
  = user_display_image(current_user)

  .info
    .name
      = current_user.name.presence

    .desc
      = current_user.email
      /= current_user.try(:role).presence || "Użytkownik"
  
  .logout
    = link_to fa_icon("sign-out"), destroy_user_session_path, method: :delete
