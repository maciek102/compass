= render "product_categories/page_headers/index"

/ przełączanie widoków (scope) tabeli
= render Tables::ViewModeSwitcherComponent.new(view_modes: @view_modes,base_params: params)

= render Tables::IndexTableComponent.new(list: @product_categories, turbo_id: "categories-table-body")